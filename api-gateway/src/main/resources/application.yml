server:
  port: 6010
spring:
  cloud:
    gateway:
      routes:
        -
          id: reservation-service
          predicates:
            - Path=/reservation/**
          uri: "http://localhost:6012/"
        -
          id: user-service
          predicates:
            - Path=/user/**
          uri: "http://localhost:6011/"
    kubernetes:
      discovery:
        all-namespaces: true
#      config:
#        enabled: true